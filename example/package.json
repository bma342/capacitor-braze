{
  "name": "capacitor-braze-example",
  "private": true,
  "version": "0.0.0",
  "description": "Example Capacitor app exercising every capacitor-braze method against real Braze.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "cap:sync": "npm run build && cap sync",
    "cap:ios": "npm run cap:sync && cap open ios",
    "cap:android": "npm run cap:sync && cap open android"
  },
  "dependencies": {
    "@braze/web-sdk": "^6.8.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "capacitor-braze": "file:.."
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.0",
    "typescript": "~5.4.2",
    "vite": "^8.0.16"
  }
}
