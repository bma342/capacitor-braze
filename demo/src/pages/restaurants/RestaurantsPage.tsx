/**
 * Placeholder. Filled in during L.2 (restaurant browse flow).
 * Will list restaurants from `mockData/restaurants.ts` and fire
 * `track.verticalViewed('restaurants')` on mount.
 */
export function RestaurantsPage() {
  return (
    <div className="px-4 py-6">
      <h1 className="text-2xl font-semibold">Restaurants</h1>
      <p className="mt-2 text-sm text-neutral-500">
        Browse list lands in <code>L.2</code>. See <code>mockData/restaurants.ts</code>.
      </p>
    </div>
  );
}
